# Streamlit secrets template for production deployment
# Copy this file to secrets.toml and configure with your actual values
# DO NOT commit secrets.toml to version control

[supabase]
url = "https://your-project-id.supabase.co"
anon_key = "your-anon-key-here"

# Optional: Include service key if needed for admin operations
# service_key = "your-service-key-here"